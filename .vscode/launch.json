{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "restart": true,
            "program": "${workspaceRoot}/test/server.js",
            "console": "integratedTerminal",
            "env": {
                "LOG_LEVEL": "debug"
            }
        }
    ]
}
