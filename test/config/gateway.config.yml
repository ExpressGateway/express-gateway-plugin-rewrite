http:
  port: 8080
admin:
  port: 9876
  hostname: localhost
apiEndpoints:
  api:
    host: '*'
serviceEndpoints:
  httpbin:
    url: 'https://httpbin.org'
policies:
  - proxy
  - rewrite
pipelines:
  - name: basic
    apiEndpoints:
      - api
    policies:
      -
        rewrite:
          -
            condition:
              name: match
              match: /gigino/:code
            action:
              rewrite: /status/:code
              redirect: permanent
          -
            condition:
              name: match
              match: ^/js/(.*)$
            action:
              rewrite: /src/js/$1

      - proxy:
          - action:
              serviceEndpoint: httpbin
              changeOrigin: true
